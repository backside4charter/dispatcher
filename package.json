{
  "name": "dispatcher",
  "version": "0.3.0",
  "license": "MIT",
  "private": true,
  "type": "module",
  "description": "A stack-agnostic task dispatcher for agent-driven development: a task-board CLI (Linear or GitHub Projects), a local board-event channel, agent GitHub App identity tooling (commits, pull requests, tokens), a review-to-board sync, and a worktree pruner - shipped as one self-contained executable per platform.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/backside4charter/dispatcher.git"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run --coverage"
  },
  "dependencies": {
    "@clack/prompts": "1.7.0",
    "tsx": "4.19.4",
    "zod": "3.25.76"
  },
  "devDependencies": {
    "@types/node": "24.12.2",
    "@vitest/coverage-v8": "3.2.6",
    "typescript": "5.9.3",
    "vitest": "3.2.6"
  }
}
